const location = `---
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: [value]
  description: [value]
spec:
  targets:
    - [relative path to catalog descriptor file]
    - [URL to catalog descriptor file]`

export default location
